language: c
sudo: required

script:
  - cd tests && make && ./rtmlib_unittests && cd -
  - cd examples/ && make

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages: 
    - gcc-5 
    - g++-5

os:
  - linux
